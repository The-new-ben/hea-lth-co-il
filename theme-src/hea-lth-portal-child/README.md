# Hea-lth Portal Child

This is the production child theme for `hea-lth-portal`.

The parent theme owns the portal design system, template library, and page chrome. The child theme is intentionally thin so future production-specific overrides are isolated without changing the reusable parent package.
