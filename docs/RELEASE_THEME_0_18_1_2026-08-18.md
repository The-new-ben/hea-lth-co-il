# Release — theme 0.18.1 (version resync + pipeline credential verification)

**Date:** 2026-08-18 · **Commit:** `d7990df` · **Run:** 32128127262 (attempt 2) · **Machine:** 777 · **Operator:** Claude Fable 5

## What shipped

One fix — review finding **M7** ([CLAUDE_B2B_LAYER_REVIEW_2026-08-18.md](agent-sync/CLAUDE_B2B_LAYER_REVIEW_2026-08-18.md)): PR #28 bumped `style.css` to 0.18.0 but left `HEA_LTH_PORTAL_VERSION` at 0.17.0, so live assets cache-busted with a burned `?ver=0.17.0` and returning visitors/CDN kept stale bundles. Both bumped together to **0.18.1** per deploy-law rule 5 (`theme-src/hea-lth-portal/functions.php:16`, `theme-src/hea-lth-portal/style.css:7`).

This release was deliberately chosen as the smallest safe change to **end-to-end verify the rotated `WP_APP_PASSWORD`** on the first deploy since the owner's password rotation.

## Verification battery (machine 777, before push)

PHP contract 34/34 · node contract pass · pytest 19/19 · PHPCS exit 0 · PHPStan exit 0 · package build `hea-lth-portal-0.18.1.zip` (89 files, sha256 `774ec5bf…`) · deploy dry-run `dry_run_ok`.

## Credential story (for the record)

- Attempt 1 failed in 8s at the first WordPress call: `401 incorrect_password` — "הסיסמה שסופקה היא סיסמת אפליקציה שגויה". The owner's first secret value was not a valid Application Password (login password pasted, most likely). **Zero site impact** — failure preceded any install; live stayed on `gh-41a341f3…`.
- Owner created a fresh Application Password in wp-admin and re-set the env-`production` secret; failed job rerun succeeded in 42s (plugin overwrite-install 0.18.0, parent theme 0.18.1, child theme re-activated, temp route removed).

## Live verification (eyes, post-deploy)

- Platform healthcheck: `0.18.0`, deployment_id `gh-d7990dfc…-32128127262-2` ✓ flipped
- Portal-child healthcheck: deployment_id `…-2-child` ✓ flipped
- Homepage 200, full HTML 105,242 bytes: `portal.css?ver=0.18.1` ✓ · `templates.css?ver=0.18.1` ✓ · `ver=0.17.0` occurrences: **0** ✓ · both theme markers present ✓

## State after this release

- **The deploy pipeline is fully operational with the rotated credential.** Dependabot PRs #29 (axe-core), #16 (phpstan 2.2.8), #12 (actions/checkout) are now safe to merge — each fires a deploy run; owner's call on timing. After merging #16, rerun `composer install` in `tooling/php-quality` locally.
- Next work, per the review's proposed triage: C1 (server-side `get_temp_dir()` check), then the lead-funnel hardening bundle (C2+C3+M4), then the YMYL honesty pass (H1+H7+M8) — priorities await the owner.
