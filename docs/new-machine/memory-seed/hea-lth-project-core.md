---
name: hea-lth-project-core
description: "Core facts about the hea-lth.co.il portal project — repo, live state, deploy triggers, PHP constraint"
metadata: 
  node_type: memory
  type: project
  originSessionId: cce6e85a-fbb7-4c00-af47-0518f4d23aae
---

Hea-lth.co.il is an institute-grade Hebrew (RTL) private-health portal. GitHub: The-new-ben/hea-lth-co-il (private, default branch main); gh CLI is authenticated as The-new-ben with repo+workflow scopes. The local folder C:\Users\pro\Documents\websites\hea-lth-co-il is shared with OpenAI Codex (git user "Codex", branches codex/*). As of 2026-07-16 the portal is LIVE at release cd29086 (theme 0.7.1, plugin 0.7.0): **the body-map index** — a rich Leaflet/OSM care map attached under the 3D model (homepage hero + /anatomy/) with 630 real IL hospitals+clinics (baked attributed dataset assets/data/healthcare-poi-il.json), visitor geolocation, and **client #1 Dr. Keren Chaya Cohen** (plastic surgery, Ramat Aviv, drkerencohen.co.il) as a disclosed premium H-pin in plugin data/default-map-manifest.json (more clients = more JSON entries through the same gate). Body selections drive the map (structure→region translation table duplicated in care-map.js + engagement.js — keep in sync). Map gate extended keyless (leaflet-osm; tiles fixed in code; google path intact). Viewer rotation locked horizontal-only (owner-authorized freeze exception in anatomy-three-viewer.js). 37 clickable structures incl. hand/foot/nose/lips. WhatsApp consult bar live (972525101555, page-context prefill). Engagement layer: click→info card (labels come FLATTENED as `label` from the gate, `labels.he` only in raw manifests). Earlier: 2026-07-15 release 654266e (theme 0.5.0, plugin 0.5.1): layered real-body 3D figure (skin+muscles+skeleton, z-anatomy-layered-v2) above the fold with click→services; native accessibility panel + statement at /accessibility/ (pojo retired); site identity "Hea-lth" everywhere incl. the homepage title "Hea-lth — מרכז בחירה לרפואה פרטית בישראל" (static front page ⇒ Yoast uses the PAGE's _yoast_wpseo_title, not the home template — that meta is where the homepage title lives). **The COMPLETE route map is provisioned — all 22 foundation pages, zero 404s**: 7 original + accessibility + hubs on template-hub.php (diagnostics +imaging/laboratory/second-opinion, wellness +prevention, private-medicine) + about/editorial-policy/privacy/terms + noindex holding pages (contact, professionals/profile-update, account). /skin/ intentionally NOT provisioned (legacy 301 exists). Provisioning is versioned in class-hea-lth-page-provisioner.php (bump BLUEPRINT_VERSION to re-run; never overwrites existing pages; tolerant option updates guard against Elementor's throwing listeners). Self-hosted Noto Hebrew webfonts (no Google CDN). **Yoast SEO v28 active and configured** — theme's inc/portal-seo.php defers (contract-tested). Still active in wp-admin (owner decisions): WooCommerce (kept — owner wants a medical-equipment store; assets dequeued on portal pages) and **Elementor (do NOT deactivate blindly — legacy commercial pages may depend on it)**. GOTCHA: untracked files under theme-src/ get packaged by the deploy build — keep staged/unwired assets in tmp/ (future 3D layers wait in tmp/z-anatomy/staged-layers/). Healthchecks: /wp-json/hea-lth-portal/v1/healthcheck and /wp-json/hea-lth-platform/v1/healthcheck.

**Deploy danger:** push to main touching theme-src/**, plugin-src/**, deploy/**, scripts/**, tooling/php-quality/**, tooling/web-quality/**, tests/test_wordpress_pipeline.py, .gitleaks.toml, or .github/workflows/wordpress-deploy.yml deploys to PRODUCTION. docs/**, design-lab/**, tooling/tests/**, AGENTS.md, CLAUDE.md do not. Production host (UPress) runs PHP 7.4.33 — shipped PHP must stay 7.4-compatible; local PHP is 8.3. Secrets only in GitHub Actions (WP_BASE_URL, WP_USER, WP_APP_PASSWORD; environment "production") — never in files or chat.

Shared agent contract lives in AGENTS.md at repo root (CLAUDE.md imports it) since commit ec1ba14, which also synced all docs/, design-lab/, tooling/tests (22 contract tests), and tooling/theme-preview into git. Related: [[hea-lth-3d-gate]], [[hea-lth-owner-style]].

**Standing deploy authorization (owner, 2026-07-16): "always deploy don't ask."** Deploy without per-release confirmation prompts; the full verification battery + live verification + honest reporting still mandatory; still ASK for destructive/irreversible actions beyond a routine release (gate weakening, data loss, spending). Recorded in repo: docs/agent-sync/OWNER_DIRECTIVE_STANDING_DEPLOY_2026-07-16.md.


**2026-08 update:** Codex shipped a B2B layer after Claude's 0.10.0: supplier showroom + portal, clinic procurement intake, brokerage ledger/agreements, equipment + RFQ marketplace, science knowledge graph (PRs 17-28, live plugin 0.18.0 as of 2026-08-18). Codex also extended Claude's metrics class (b2b_submit type) and wrapped the WhatsApp bar in an aside landmark. Check healthchecks + docs/agent-sync receipts before assuming state. A new-machine migration kit (memory seed, session prompt, runbook, committed .claude/settings.json allowlist) lives at docs/new-machine/.

**Machine migration 2026-08-18:** active dev machine is now user `777`, repo at C:\Users\777\hea-lth (memory dir C--Users-777-hea-lth). gh authed as The-new-ben (repo+workflow). Verified there: clone at edd35a2 = origin HEAD, live healthcheck 0.18.0 (deployment gh-41a341f…), node contract tests pass, pytest 19/19 pass. PHP 8.3.33 + Composer 2.10.2 installed via docs/new-machine/install-php-toolchain.ps1 into C:\Users\777\tools\php-8.3 (user PATH; Claude's shells inherit a stale PATH — call C:\Users\777\tools\php-8.3\php.exe by full path). winget is chronically broken for PHP (manifest 404s after each patch) — always use that script. Full battery GREEN on this machine 2026-08-18: PHP contract 34/34, node pass, pytest 19/19, PHPCS exit 0, PHPStan exit 0. Deploy secrets live in the GitHub **environment `production`** — use `gh secret set … --env production` (repo-level set does NOT override). WP_APP_PASSWORD there last set 2026-07-10; owner rotated all passwords ~2026-08-18, so it must be re-set by the owner before the next deploy, and the first deploy is the end-to-end verification of it. Claude's first review of the Codex B2B layer (PRs 17-28) is receipted in docs/agent-sync/CLAUDE_B2B_LAYER_REVIEW_2026-08-18.md: 3 critical (brokerage-doc disk exposure risk, single-unlogged-mail lead funnel, cached-nonce silent form death), 8 high (self-certified YMYL review stamps, script-"verified" real suppliers, missing info-boundary on all new surfaces, parallel lead-release gate, REST/sitemap exposure), theme version split 0.17.0/0.18.0 live. NO fixes applied yet — triage waits on owner priorities; dependabot PRs #29/#16/#12 held until the secret is re-set (their paths trigger deploys). The M7 version-constant bump is the designated first pipeline-verification deploy.